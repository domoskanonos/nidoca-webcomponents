{
  "name": "@domoskanonos/nidoca-showcase",
  "version": "1.0.0",
  "license": "MIT",
  "description": "nidoca framework (nidoca-showcase)",
  "devDependencies": {
    "html-webpack-plugin": "5.2.0",
    "terser-webpack-plugin": "^5.1.1",
    "prettier": "^2.2.1",
    "prettier-plugin-organize-imports": "^1.1.1",
    "ts-loader": "8.0.17",
    "typescript": "^4.1.4",
    "webpack": "^5.4.0",
    "webpack-cli": "4.5.0"
  },
  "dependencies": {
    "@domoskanonos/frontend-basis": "latest",
    "@domoskanonos/nidoca-core": "latest"
  },
  "author": "Dominik Bruhn",
  "scripts": {
    "build": "cd .. && cd generator && npm install && npm run generate-showcase && cd.. && cd showcase && npm install && prettier --write \"./**/*.{ts,json}\" && tsc -p ./",
    "bundle": "npm run build && webpack"
  },
  "browserslist": [
    "last 1 Chrome version"
  ]
}
